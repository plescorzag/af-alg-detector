apiVersion: v1
kind: Pod
metadata:
  name: afalg-test
spec:
  restartPolicy: Never
  containers:
    - name: test
      image: registry.redhat.io/ubi9/python-39
      command:
        - python3
        - -c
        - |
          import socket
          socket.socket(38, socket.SOCK_SEQPACKET, 0)
